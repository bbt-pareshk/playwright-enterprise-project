import { ENV } from '../../../config/env';
import { ROLES, UserRole } from '../constants/roles';

export const users: Record<UserRole, any> = {
  [ROLES.LEADER]: ENV.USERS.LEADER,
  [ROLES.MEMBER]: ENV.USERS.MEMBER,
  [ROLES.LEADER_ACTIVE_HOSTING_PLAN]: { 
    username: ENV.LEADER_ACTIVE_HOSTING_PLAN_USERNAME, 
    password: ENV.LEADER_ACTIVE_HOSTING_PLAN_PASSWORD 
  },
  [ROLES.LEADER_MULTI_GROUP_HOSTING_PLAN]: { 
    username: ENV.LEADER_MULTI_GROUP_HOSTING_PLAN_USERNAME, 
    password: ENV.LEADER_MULTI_GROUP_HOSTING_PLAN_PASSWORD 
  },
  [ROLES.LEADER_FUNCTIONAL]: { 
    username: ENV.LEADER_FUNCTIONAL_USERNAME, 
    password: ENV.LEADER_FUNCTIONAL_PASSWORD 
  },
};
